import ReactDOM from "react-dom";
import React from "react";
import App from "./components/App";

// Directly render the App component without authentication
ReactDOM.render(
  <App />,
  document.getElementById("root")
);